# 介绍

## 什么是 @ldesign/testing？

`@ldesign/testing` 是一个完整的企业级测试工具集，整合了现代化的测试框架和工具，为开发者提供一站式的测试解决方案。

## 核心特性

### 🧪 全面的测试类型支持

- **单元测试** - 基于 Vitest 的快速单元测试
- **E2E 测试** - Playwright 支持的端到端测试
- **组件测试** - Vue、React 组件测试
- **API 测试** - RESTful API 测试
- **集成测试** - 模块间集成测试
- **性能测试** - 基准测试和性能分析

### 🔄 强大的 Mock 系统

- **12+ 预定义数据类型** - user, product, company, event 等
- **Faker.js 集成** - 生成真实的假数据
- **MSW 支持** - 网络请求拦截和 Mock
- **关联数据生成** - 自动生成带关系的测试数据

### 📊 智能覆盖率分析

- **详细报告** - 行、语句、函数、分支覆盖率
- **智能分析** - 自动评分和改进建议
- **阈值验证** - 确保代码质量标准
- **趋势分析** - 跟踪覆盖率变化

### ⚡ 性能优化

- **并行测试** - 多进程并行执行
- **智能缓存** - 加速测试运行
- **增量测试** - 只运行受影响的测试

### 🤖 自动化工具

- **测试生成** - 自动生成测试文件
- **配置模板** - 开箱即用的项目配置
- **CLI 工具** - 丰富的命令行功能

## 设计理念

### 简单易用

我们相信测试应该简单直接。`@ldesign/testing` 提供了清晰的 API 和丰富的文档，让你快速上手。

```typescript
// 简单的测试
import { describe, it, expect } from 'vitest'

describe('Calculator', () => {
  it('adds numbers', () => {
    expect(1 + 1).toBe(2)
  })
})
```

### 功能完整

一个工具解决所有测试需求，不需要配置多个工具和插件。

```typescript
// 所有功能都来自同一个包
import {
  describe, it, expect,           // 测试框架
  mockFactory,                     // Mock 数据
  waitFor, retry,                  // 测试工具
  assertNotNullish, assertArray,   // 自定义断言
  BenchmarkRunner                  // 性能测试
} from '@ldesign/testing'
```

### TypeScript 优先

完整的 TypeScript 支持，享受类型安全和智能提示。

```typescript
import { TestDataBuilder } from '@ldesign/testing'

interface User {
  id: string
  name: string
  email: string
}

const user = new TestDataBuilder<User>()
  .with('id', '123')
  .with('name', 'John')
  .with('email', 'john@example.com')
  .build()
```

### 可扩展

提供灵活的配置和扩展点，适应不同项目需求。

```typescript
import { defineConfig } from '@ldesign/testing'

export default defineConfig({
  // 自定义配置
  unit: {
    timeout: 10000,
    clearMocks: true
  },
  coverage: {
    threshold: {
      branches: 90,
      functions: 90
    }
  }
})
```

## 技术栈

`@ldesign/testing` 建立在以下优秀的开源项目之上：

- **[Vitest](https://vitest.dev/)** - 快速的单元测试框架
- **[Playwright](https://playwright.dev/)** - 可靠的端到端测试
- **[Faker.js](https://fakerjs.dev/)** - 假数据生成
- **[MSW](https://mswjs.io/)** - API mocking
- **[Tinybench](https://github.com/tinylibs/tinybench)** - 性能基准测试

## 与其他工具的比较

### vs Jest

- ✅ 更快的测试执行速度（基于 Vite）
- ✅ 原生 ESM 支持
- ✅ 内置 TypeScript 支持
- ✅ 更好的开发体验

### vs Testing Library

- ✅ 包含但不限于组件测试
- ✅ 集成了更多测试工具
- ✅ 统一的 API 和配置

### vs Cypress

- ✅ 更快的 E2E 测试（基于 Playwright）
- ✅ 多浏览器支持
- ✅ 更好的调试体验
- ✅ 支持更多测试类型

## 适用场景

### ✅ 适合

- 中大型项目
- 需要全面测试覆盖
- TypeScript 项目
- 前端框架项目（Vue、React）
- Node.js 后端项目
- 组件库开发

### ⚠️ 可能不适合

- 纯粹的单元测试项目（直接使用 Vitest 可能更简单）
- 需要特殊测试框架的项目
- 有严格的依赖限制的项目

## 下一步

- [快速开始](/guide/getting-started) - 5 分钟上手
- [配置指南](/guide/configuration) - 了解配置选项
- [示例](/examples/basic) - 查看实际示例

## 获取帮助

遇到问题？我们随时为你提供帮助：

- 📖 查看 [API 文档](/api/overview)
- 💬 提交 [GitHub Issue](https://github.com/ldesign/testing/issues)
- 🤝 查看 [贡献指南](https://github.com/ldesign/testing/blob/main/CONTRIBUTING.md)
